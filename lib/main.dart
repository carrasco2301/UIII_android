import 'package:flutter/material.dart';
import 'frm-contacto.dart';
void main() {
  runApp(
    new MaterialApp(
      title: 'Ejemplo de flutter',
      theme: ThemeData(
      primarySwatch: Colors.blueGrey),
      home: PaginaContacto()
    ) //fin de material
  ); //fin de runAPP
} //fin de main
